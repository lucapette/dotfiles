require 'logger'

def enable_logger
 log_to(Logger.new(STDOUT))
end

def disable_logger
  log_to(nil)
end

def log_to(logger)
 ActiveRecord::Base.logger = logger
 ActiveRecord::Base.clear_active_connections!
end

require 'rubygems'
require 'hirb'

Hirb.enable
enable_logger
